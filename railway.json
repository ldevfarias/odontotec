{
  "$schema": "https://schema.railway.com/railway.json",
  "build": {
    "buildCommand": "npm install --include=dev && cd apps/odonto-api && npm run build && npm run sync-db"
  },
  "deploy": {
    "startCommand": "node apps/odonto-api/dist/src/main"
  }
}
