/**
 * Centralized configuration for the landing app.
 * Always use APP_URL when linking to the odonto-front application.
 */
export const APP_URL = process.env.NEXT_PUBLIC_APP_URL || 'http://localhost:3001';
