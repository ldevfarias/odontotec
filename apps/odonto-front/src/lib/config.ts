/**
 * Centralized configuration for the odonto-front app.
 * Always use API_URL when needing the base backend URL.
 */
export const API_URL =
  process.env.NEXT_PUBLIC_API_URL || 'http://localhost:3000';
